import React from "react";
import ReactDOM from "react-dom";

const App = () => (
  <div>
    <h1>React with Parcel and Typescript</h1>
  </div>
);

ReactDOM.render(<App />, document.getElementById("app"));
